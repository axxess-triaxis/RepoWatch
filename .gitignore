# Python
__pycache__/
*.pyc
*.egg-info/
.venv/
venv/
build/
dist/
.pytest_cache/

# OS / editors
.DS_Store
.vscode/
.idea/

# Local scratch outputs from ad hoc runs -- the real, committed dogfooding
# report lives at docs/repowatch_report.{json,html} (tracked deliberately,
# not excluded by this pattern since it's anchored to the repo root).
/repowatch_report.json
/repowatch_report.html
